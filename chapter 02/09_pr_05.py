# program to find a square of a number entered by a user

a = input("Enter a first number : ")
# We can change it into an integer bcz input function stores data in strings
a = int(a)
square = (a*a)
print("square of a is", square)
